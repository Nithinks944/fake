name: CI
on:
  push:
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate local test coverage rule
        shell: pwsh
        run: ./check-test-coverage-rule.ps1
